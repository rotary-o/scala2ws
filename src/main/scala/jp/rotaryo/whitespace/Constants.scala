package jp.rotaryo.whitespace

private[whitespace] object Constants {

  val minusOne = BigInt(-1)
  val zero = BigInt(0)
  val one = BigInt(1)
  val two = BigInt(2)
  val charMax = BigInt(256)
  val intMax = BigInt(Int.MaxValue)
}
